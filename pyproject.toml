[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fensoundswitch"
version = "0.1.0"
description = "FenSoundSwitch route-based monitor and network receiver volume controller."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "paho-mqtt==2.1.0",
    "pywebview==6.1",
    "monitorcontrol==4.2.0; sys_platform == 'win32'",
    "pythonnet==3.0.5; sys_platform == 'win32'",
]

[project.optional-dependencies]
build = [
    "Nuitka==4.2",
]

[tool.setuptools]
py-modules = [
    "app",
    "app_version",
    "audio_outputs",
    "core_audio",
    "config_archive",
    "default",
    "autostart",
    "diagnostics",
    "gui",
    "ddc",
    "settings",
    "theme",
    "plugin_api",
    "plugin_hotkeys",
    "plugin_manager",
    "native_platform",
    "macos_platform",
    "macos_plugin_hotkeys",
    "windows_platform",
    "web_presentation",
    "web_ui_host",
]

[tool.setuptools.package-data]
web = ["*.html", "*.css", "*.js"]

[tool.setuptools.packages.find]
include = ["plugins", "plugins.*", "web"]
